import members from "./members";
import pass from "./Randompws";

let randomPasswords = () => {
  members.map(
    (person.password = () => {
      for (let i = 0; i < 15; i++) {
        Random += [getrandomPassword()];
        console.log(getrandomPassword());
      }
    }),
  );
};

let getrandomPassword = () => {
  return Math.floor(Math.random() * pass.length);
};

let Random = "";

randomPasswords();
